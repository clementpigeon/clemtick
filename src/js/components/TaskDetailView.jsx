const React = require('react');
const Panel = require('react-bootstrap/lib/Panel');


let TaskDetailView = React.createClass({
  getInitialState() {
    return {};
  },

  componentDidMount() {
  },

  render() {
    return (
      <Panel>
      Basic panel example
      </Panel>
    );
  }
});

module.exports = TaskDetailView;
